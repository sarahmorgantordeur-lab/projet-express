---

# **Mini-cours : API Todo en Node.js avec Express (Architecture MVC + Services)**

---

## **1️⃣ Concepts de base**

### **Node.js**

* Plateforme qui permet d’exécuter du JavaScript côté serveur.
* Idéal pour créer des API REST, serveurs web, ou manipuler des fichiers et bases de données.

### **Express**

* Framework minimaliste pour Node.js.
* Simplifie la création de routes, la gestion des requêtes et réponses HTTP, et l’organisation du code.

### **API REST**

* API = Application Programming Interface
* REST = Représentation State Transfer (manière standardisée d’échanger des données via HTTP)
* Méthodes principales :

  * **GET** → récupérer des données
  * **POST** → créer des données
  * **PUT / PATCH** → modifier des données
  * **DELETE** → supprimer des données

Dans ton projet :

* `GET /api/todos` → récupérer toutes les tâches
* `POST /api/todos` → créer une nouvelle tâche

---

## **2️⃣ Architecture MVC + Service**

### **M : Modèle**

* Contient les **données brutes** et la logique de manipulation des données.
* Dans ton projet : `src/models/todo.model.js`

  * Tableau `todos` en mémoire pour stocker les tâches
  * Méthodes :

    * `findAll()` → retourne toutes les tâches
    * `create(todo)` → ajoute une tâche

### **V : Vue**

* Pour une API, la “vue” n’est pas un fichier HTML, mais la **réponse JSON envoyée au client**.
* Express gère ça via `res.json()`.

### **C : Contrôleur**

* Reçoit les requêtes HTTP, appelle le **service**, puis renvoie la réponse.
* Exemple : `getAllTodos(req, res)` ou `createTodo(req, res)`
* Il ne touche jamais directement au modèle ou à la base de données, il délègue au service.

### **Service**

* Contient la **logique métier**, c’est-à-dire les règles qui régissent ton application.
* Exemple : **titre obligatoire pour créer une tâche**
* Sépare le “quoi faire” (service) du “comment le recevoir/envoyer” (contrôleur)

---

## **3️⃣ Comment ça fonctionne dans ton projet**

1. **Le client** (navigateur, Postman, curl) envoie une requête HTTP :

   * `GET /api/todos` ou `POST /api/todos`
2. **Express** reçoit la requête et la dirige vers la **route correspondante** (`todo.routes.js`)
3. La **route** appelle le **contrôleur**
4. Le **contrôleur** appelle le **service**
5. Le **service** applique les **règles métier**
6. Le service utilise le **modèle** pour accéder aux données (ici le tableau `todos`)
7. Les données sont retournées au **contrôleur**, qui les envoie au client en **JSON**

---

## **4️⃣ Avantages de cette approche**

1. **Séparation des responsabilités**

   * Modèle = données
   * Service = règles métier
   * Contrôleur = interface HTTP
   * Route = URL
     → Plus facile à maintenir et à étendre

2. **Réutilisabilité**

   * Le service peut être utilisé par plusieurs contrôleurs ou même d’autres projets

3. **Testabilité**

   * On peut tester chaque partie séparément (unit tests)
   * Exemple : tester `TodoService.createTodo()` sans lancer le serveur

4. **Lisibilité et organisation**

   * Les projets restent clairs même quand ils deviennent grands

5. **Évolutivité**

   * Facile de remplacer le modèle mémoire par une vraie base de données (MongoDB, PostgreSQL…) sans changer le reste du code

---

## **5️⃣ Exemple concret : ce que tu as fait**

* Création d’un **API Todo simple** avec Node.js et Express
* Architecture **MVC + service** :

  * `src/models/todo.model.js` → données
  * `src/services/todo.service.js` → règles métier
  * `src/controllers/todo.controller.js` → traitement des requêtes
  * `src/routes/todo.routes.js` → mapping URL → contrôleur
* Test via **curl / Postman / navigateur**
* Gestion des erreurs (règle métier : titre obligatoire)
* Réponse en **JSON** pour l’API REST

---

### 🔹 Schéma simplifié

```
[ Client / Postman / curl ]
             |
             v
        [ Routes ] ---------> (GET / POST)
             |
             v
       [ Controller ]
             |
             v
        [ Service ] --- règles métier
             |
             v
        [ Model ] --- accès aux données
             |
             v
        [ JSON Response ] → Client
```

---

💡 **Résumé pédagogique**

* Tu as construit une **API REST complète** en Node.js
* Tu as appris à **séparer le code** proprement (MVC + service)
* Tu sais tester via **terminal ou Postman**
* Tu peux **étendre le projet facilement** pour ajouter une DB, de nouvelles routes, ou d’autres règles métier

---

